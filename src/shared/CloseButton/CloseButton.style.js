import styled from 'styled-components'

const IconWrapper = styled.button`
  background-color: transparent;
  padding: 0;
  border: 0;
`

const closeIconStyles = {
  cursor: 'pointer',
}

export { IconWrapper, closeIconStyles }
